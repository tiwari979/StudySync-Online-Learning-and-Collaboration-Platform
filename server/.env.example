# Copy this file to `.env` and fill in the values.
# Do NOT commit your real `.env` to git. The repo's .gitignore already includes .env

# MongoDB Atlas connection string example (replace <username>, <password>, <dbname>):
MONGO_URI=mongodb+srv://hamzaWeb:Ha1220042@@cluster0.1ga8zab.mongodb.net/

# Client URL used for CORS
CLIENT_URL=http://localhost:5173

# Server port
PORT=5000

# Optional: JWT secret
JWT_SECRET=your_jwt_secret_here

# Optional: PayPal and Cloudinary (replace when needed)
PAYPAL_CLIENT_ID=
PAYPAL_SECRET_ID=
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
